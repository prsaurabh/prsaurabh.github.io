# Analysis and prep files
resume/
resume-analysis/
drafts/

# Claude Code
.claude/
.env

# Website encryption password (never commit — single source of truth for all builders)
.staticrypt_pw

# Build staging (unencrypted HTML — never commit)
dashboard/_src/
prep/_src/
